apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Name }}-tempo-pv
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual-tempo
  hostPath:
    path: /data/{{ .Release.Name }}-tempo
